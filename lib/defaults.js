/**
 * SolidError defaults values.
 * @module lib/defaults
 * @version 0.1.0
 * @since 0.2.0
 */
module.exports = {
  /**
   * Define if SolidError should rendere colors.
   * @readnly
   * @const {boolean}
   * @version 0.1.0
   * @since 0.1.0
   * @version 0.1.0
   * @since 0.1.0
   */
  COLORS: true,
  /**
   * Defines the error column width when wordwrap is active.
   * @readonly
   * @const {number}
   * @version 0.1.0
   * @since 0.1.0
   */
  COLUMNS: 80,
  /**
   * Defines the footer section divider style.
   * @readonly
   * @const {string}
   * @version 0.1.0
   * @since 0.1.0
   */
  FOOTER_STYLE: '-',
  /**
   * Defines the header section divider style.
   * @readonly
   * @const {string}
   * @version 0.1.0
   * @since 0.1.0
   */
  HEADER_STYLE: '=',
  /**
   * Defines the header section title.
   * @readonly
   * @const {string}
   * @version 0.1.0
   * @since 0.1.0
   */
  HEADER_TITLE: 'ERROR',
  /**
   * Defines the example section divider style.
   * @readonly
   * @const {string}
   * @version 0.1.0
   * @since 0.1.0
   */
  EXAMPLE_STYLE: '-',
  /**
   * Defines the example section title.
   * @readonly
   * @const {string}
   * @version 0.1.0
   * @since 0.1.0
   */
  EXAMPLE_TITLE: 'EXAMPLE',
  /**
   * Defines the inner section divider style.
   * @readonly
   * @const {string}
   * @version 0.1.0
   * @since 0.1.0
   */
  INNER_STYLE: '-',
  /**
   * Defines the inner section title.
   * @readonly
   * @const {string}
   * @version 0.1.0
   * @since 0.1.0
   */
  INNER_TITLE: 'INNER ERROR',
  /**
   * Define if code highlight is active.
   * @readnly
   * @const {boolean}
   * @version 0.1.0
   * @since 0.1.0
   */
  HIGHLIGHT: true,
  /**
   * Define if markdown support is active.
   * @readnly
   * @const {boolean}
   * @version 0.1.0
   * @since 0.1.0
   */
  MARKDOWN: true,
  /**
   * Define if wordwrap is active
   * @readnly
   * @const {boolean}
   * @version 0.1.0
   * @since 0.1.0
   */
  WORDWRAP: true,
  /**
   * Define the default language
   * @readnly
   * @const {string}
   * @version 0.1.0
   * @since 0.1.0
   */
  LANG: 'en',
  /**
   * Define the code for unexpected errors
   * @readnly
   * @const {string}
   * @version 0.1.0
   * @since 0.1.0
   */
  EUNX: 'EUNX'
}
